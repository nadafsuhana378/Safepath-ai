import React from "react";
import ReactDOM from "react-dom/client";
import SafePathPrototype from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SafePathPrototype />
  </React.StrictMode>
);
